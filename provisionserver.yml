---

- hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Add Hosts to memory 
      add_host:
        name: "{{ servername }}"
#        group: "{{ provisioned_vms }}

#    - name: Run Post Provision tasks
#      hosts: {{ provisioned_vms }}
#      tasks:

    - name:
      include_role:
        name: compliance

    - name:
      include_role:
        name: satellite

    - name: 
      include_role:
        name: patch


